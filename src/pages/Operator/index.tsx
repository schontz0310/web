import React from 'react';

import {Container} from './styles'

const Operator: React.FC = () => {
  return (
    <Container>
      <h1>Operator</h1>
    </Container>
  );
};

export default Operator;
